# Ignore node_modules directory
node_modules/

# Ignore build directory (TypeScript output)
build/

# Ignore logs
logs
*.log
npm-debug.log*

# Ignore TypeScript cache
*.tsbuildinfo

# Ignore environment files
.env
.env.*

# Ignore coverage directories (if tests are added later)
coverage
*.lcov

# Ignore other common Node.js project artifacts
# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional ESLint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache (if used)
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file (if using Yarn)
.yarn-integrity

# parcel-bundler cache (if used)
.cache
.parcel-cache

# Next.js build output (if applicable)
.next
out

# Nuxt.js build / generate output (if applicable)
.nuxt
dist

# Gatsby files (if applicable)
.cache/

# vuepress build output (if applicable)
.vuepress/dist

# vitepress build output (if applicable)
**/.vitepress/dist

# vitepress cache directory (if applicable)
**/.vitepress/cache

# Docusaurus cache and generated files (if applicable)
.docusaurus

# Serverless directories (if applicable)
.serverless/

# FuseBox cache (if applicable)
.fusebox/

# DynamoDB Local files (if applicable)
.dynamodb/

# TernJS port file (if applicable)
.tern-port

# Stores VSCode versions used for testing VSCode extensions (if applicable)
.vscode-test

# yarn v2 (if applicable)
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
